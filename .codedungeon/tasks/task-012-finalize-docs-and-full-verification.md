# TASK-012: Finalize docs and full verification

PROJECT_RULES_STATUS: approved
PROJECT_RULES_DIGEST: be792d27d9969ae5ed95a5caa5de79b0b97d20bbd97ec73bf92cd031941e9d31
PROJECT_RULES_READ: yes

Repo: .
Kind: test
Wave: 10
Parallel Group: final-qa
Owner Role: qa
Depends On: TASK-007, TASK-011

## Objective
Update documentation with exact commands, run all available verification, scan tracked files for secrets, refresh project rules only if needed, and proceed to CodeDungeon finalization only on PASS.

## Context
- Final CodeDungeon PR Report may claim COMPLETE only when Verification: PASS.
- If project rules become stale only because requested source files were created, refresh and compact them before finalizing.
- CodeDungeon review and final reports must be generated by CLI commands, not written manually.

## Write Scope
- README.md
- .codedungeon/project-rules.md
- .codedungeon/project-rules.compact.md

## Acceptance Criteria
- README lists exact setup, environment, backend run, frontend run, backend test, frontend test, build, Playwright, and troubleshooting commands.
- Backend formatting, linting, build, and tests pass, or unavailable commands are recorded with concrete causes.
- Frontend linting, tests, build, and Playwright smoke pass, or unavailable commands are recorded with concrete causes.
- Tracked-file secret scan finds no real OpenRouter key.
- CodeDungeon review evidence and final report are generated through codedungeon CLI finalization commands.

## Verification Commands
- cargo fmt --manifest-path backend/Cargo.toml --all -- --check
- cargo clippy --manifest-path backend/Cargo.toml --all-targets -- -D warnings
- cargo test --manifest-path backend/Cargo.toml
- cargo build --manifest-path backend/Cargo.toml
- npm --prefix frontend run lint
- npm --prefix frontend run test
- npm --prefix frontend run build
- npm --prefix frontend run test:e2e
- powershell -NoProfile -Command "$prefix = 'OPENROUTER' + '_API_KEY=.*'; $pattern = $prefix + 'sk-|' + $prefix + 'or-'; git grep -I -n $pattern -- .; if ($LASTEXITCODE -eq 1) { exit 0 } elseif ($LASTEXITCODE -eq 0) { exit 1 } else { exit $LASTEXITCODE }"

## Risk Notes
- Final verification may expose environment setup issues that need documented remediation.
- Project rules refresh should be limited to staleness caused by this requested source-file creation.
